---
name: project-test-batch-20260902-v2
description: 2026-09-02 배치 재검증(v2) — 개정된 FN-02/FN-03/FN-03.5/FN-04(위임전 채번·요약금지·파일기반 길이검증·#표기)이 실제로 결함을 막았는지 확인. 하네스 자신의 사전 판단이 틀렸던 사례(옹고집전 총서) 발견.
metadata:
  type: project
---

동일한 6행 입고 목록(batch_20260902.md)으로 개정된 정의(b-04-h/b-04-w)를 재검증. INSERT는 이번에도 미실행(SELECT만), would-be INSERT문만 출력. 산출물: `outputs/20260902_b04h_batch_test/v2/20260902_자료조직_batch001.txt` + job1~5_raw.txt(워커 원문 그대로 저장한 대조용 파일).

**세 가지 이전 결함이 이번엔 재발하지 않았음(실측 확인):**
1. **008 40자** — job1/2/3/5 전부 개별 raw 파일과 최종 배치 파일 양쪽에서 node 스크립트로 실측 40자 확인(008/38-39=`##` 끝맺음). 이전 배치는 "40자로 인증했으나 파일엔 38자"였던 사고였는데, 이번엔 워커 응답을 **손으로 옮기지 않고 그대로 파일에 써서** 검증해 재발 없음.
2. **056 `$26`** — 4건 전부 `$2`와 `6` 사이 공백 없이 일치.
3. **옹고집전 총서 권차** — 결과적으로 **붙었다**(call_no="813.508 우239창 8", vol="8", 490/830 존재). 단, 이 과정에서 **하네스가 처음에 낸 지시가 틀렸었다**(아래 핵심 발견 1번).

**핵심 발견 — 이번 회차에서 새로 드러난 지점:**

1. **하네스의 사전 판단(위임 전 채번 단계)이 실제로 틀릴 수 있고, 워커가 이를 실측으로 정정한 사례.** 입고 목록의 총서란이 "-"라는 이유만으로 "EM52629(813.508 우239창 8)는 무관한 별개 총서이니 총서 계승 금지"라고 워커에게 지시했으나, 워커가 Supabase 직접 조회(EM52629.author="박철 글 ; 조혜란 그림" — 이번 입고본과 완전 일치)와 창비 공식 사이트 확인으로 "이 우239창 시리즈의 정식 명칭이 '재미있다! 우리 고전'이고 이번 책이 바로 그 8권의 재출간판"임을 밝혀 하네스 지시를 거스르고 권차를 계승했다. **하네스가 사후 재검증(SQL로 author 일치·17건 전량 창비 발행 확인)해 워커 판단이 옳았음을 인정하고 채택함.** 시사점: FN-02가 "위임 전 총서정보를 하네스가 미리 판단해 전달"하도록 요구하지만, **하네스의 사전 판단은 확정이 아니라 가설로 전달해야 하며, 워커가 반대 근거를 대며 이의를 제기하면 하네스가 반드시 재검증해야 한다.** 이 왕복 구조(하네스 가설 전달 → 워커 독립 검증 → 하네스 재검증 → 최종 채택)가 정의(FN-02)에 명문화되어 있지 않다. 정의 개정 시 "하네스가 전달하는 총서/선례 정보는 권고이며, 워커가 반박 근거를 제시하면 하네스가 그 근거를 직접 재조회해 최종 판단한다"를 FN-02나 FN-03에 추가할 필요가 있어 보임.
2. **입고 목록의 "총서: -"는 "총서 없음"의 신뢰할 수 있는 신호가 아니다.** SEOJI/사서가 총서란을 비워도 실제로는 총서에 속한 재출간판일 수 있음(이번 옹고집전이 그 예). 총서 여부 최종 판단은 입고 목록 텍스트가 아니라 **저자·삽화가·제목이 일치하는 자관 기존 소장이 있는지 조회**로 해야 하며, 이는 워커의 Step 2-1 범위이자 지금은 하네스도 위임 전 참고용으로 미리 조회해 전달하는 것이 안전함(이번엔 사전 조회를 했지만 해석을 잘못함 — "출판사가 다르다"고 잘못 판단했는데 실제로는 같은 창비였음. 발행처 필드까지 직접 비교하지 않고 총서명 문자열만 보고 결론 내린 것이 실수 원인).
3. **060 자관 SendMessage 재요청 없이 4건 모두 1차 통과.** 요약 금지 문구를 위임 프롬프트에 처음부터 넣은 결과 이번엔 "요약만 반환" 사고가 한 건도 없었음(직전 배치 Job3에서 발생했던 문제가 재발 안 함) — FN-02 개정이 효과적이었음을 확인.
4. **049/090 090 청구기호에 vol을 넣지 않는 정책이 실제로 헷갈리는 지점.** 옹고집전(job3)은 090="813.508 $b우239창"(vol 없음)이지만 장서 DB `call_no` 컬럼(사람이 읽는 표기, DB 반영용)에는 "813.508 우239창 8"(vol 포함)로 별도 조립해야 함 — 090 KORMARC 필드와 DB call_no 컬럼이 "값은 같은 계열이지만 vol 포함 여부가 다른" 두 개의 산출물이라는 점을 하네스가 INSERT문 작성 시 매번 직접 조립해야 했음(자동 파생 로직이 정의에 없음).
5. **Job2(놀이터)의 대상자료실 미기재 처리는 v1과 동일하게 "카탈로징 진행 + DB반영만 보류 + 번호는 예약 유지(회수 아님)"를 재적용.** 정의 개정 후에도 이 판단 자체는 정의에 명문화되지 않은 하네스 재량으로 남아 있음(FN-06 표는 "장서 DB 반영 보류"라고만 하고 번호 회수/예약 중 무엇을 할지는 여전히 하네스가 매번 판단).
6. **저자기호 정확성의 독립 재검증(SQL)이 4건 전부에서 실제로 유효했다** — 던295공(딜리셔스 선례), 이78놀(마지막 섬 선례), 우239창(총서 선례, 최초엔 하네스가 틀렸으나 재검증으로 정정), 고78애(370.18 세목분포·저자기호충돌 0건) 전부 하네스가 SQL로 직접 재조회해 실재를 확인함 — "워커 인용을 그대로 믿지 않는다"는 FN-03 규정이 실제로 오류(자신의 오류 포함)를 잡아내는 데 유효했음.

관련: [[project_test_batch_20260902]](v1, 결함 발견분), [[project_test_batch_20260711]](최초 테스트)
